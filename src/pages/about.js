import React from "react"
import PrimaryLayout from '../layouts/PrimaryLayout'

export default () => (
  <PrimaryLayout>
    <div className="col-xs-6">
      <h1>About us</h1>
      <p>this is an about page that looks pretty empty but its for learning purpose only so I thought it would be okay to just do this and considering it done.</p>
    </div>
  </PrimaryLayout>
)
